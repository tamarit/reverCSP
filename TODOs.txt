- fix repeated events in trace (sync)
- fix lost sync edges when reverse
- go back to certain element of the trace -> simply left as it is
- Optional: allow the internal choice as option in reverse evaluation. 
- Undo
- Padding for options when there is more than 10 options
- breakpoints: We want 3 types (forth, back, both) or only one?